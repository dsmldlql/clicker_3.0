# Автоматическое восстановление VNC

## Что было исправлено

### Проблема
При запуске ботов по одиночке через `run_bot.py`, VNC сервера (`x11vnc`) периодическипадали с ошибкой `caught signal: 11` (Segmentation Fault), но VNC Monitor не восстанавливал их автоматически.

### Причины
1. **VNC Monitor проверял не того бота** — использовался `bot_count=1`, что заставляло монитор проверять только бота 0
2. **Неподдерживаемая опция `-loglevel`** — в версии x11vnc 0.9.16 эта опция удалена
3. **Зомби процессы** — умершие процессы x11vnc становились `<defunct>` и мешали перезапуску

### Решение
1. **Добавлен параметр `bot_offset`** в `VNCHealthMonitor` для проверки конкретного бота
2. **Исправлена опция запуска x11vnc** — используется `-q` вместо `-loglevel 0`
3. **Улучшен мониторинг** — проверка каждые 10 секунд вместо 30

## Как работает автоматическое восстановление

### При запуске через `run_bot.py`

```python
# run_bot.py (обновлённая версия)
vnc_monitor = VNCHealthMonitor(
    bot_count=bot_index + 1,
    check_interval=10.0,
    bot_offset=bot_index  # ← Ключевой параметр!
)
vnc_monitor.start()
```

**Алгоритм работы:**

1. **Запуск бота:**
   - `bot.start()` запускает Xvfb, fluxbox, x11vnc, Chrome
   - VNC Monitor активируется

2. **Мониторинг (каждые 10 секунд):**
   ```
   Проверка бота {bot_index}:
   ├─ Xvfb запущен на :{100 + bot_id}? → Да/Нет
   ├─ Порт {5900 + bot_id} слушается? → Да/Нет
   └─ x11vnc процесс активен (не defunct)? → Да/Нет
   ```

3. **Обнаружение проблемы:**
   - Если Xvfb работает, но VNC мёртв → **Перезапуск VNC**
   - Если Xvfb упал → **Сигнал боту о необходимости полного перезапуска**

4. **Восстановление VNC:**
   ```bash
   # Автоматически выполняется внутри VNC Monitor:
   x11vnc -display :{100+bot_id} -rfbport {5900+bot_id} \
          -nopw -forever -shared -nowf -noxdamage -repeat -q
   ```

5. **Проверка успеха:**
   - Порт слушается? → **OK**
   - Процесс умер? → **Повтор через 10 секунд**

## Логи автоматического восстановления

### Нормальная работа
```
[+] VNC Monitor: Запущен (мониторинг бота 1 на :101 каждые 10.0 сек)
[+] VNC Monitor: Активирован (monitoring bot 1 on :101)
[*] VNC Monitor: Бот 1 OK (проверка #1)
[*] VNC Monitor: Бот 1 OK (проверка #2)
...
```

### Обнаружение проблемы и восстановление
```
[*] VNC Monitor: Бот 1 OK (проверка #5)
[!] VNC Monitor: Бот 1 - VNC не работает (порт 5901)
[*] VNC Monitor: Перезапуск x11vnc для бота 1 (дисплей :101, порт 5901)
[+] VNC Monitor: x11vnc для бота 1 успешно перезапущен на порту 5901
[*] VNC Monitor: Бот 1 OK (проверка #6)
```

### Критическая ошибка (Xvfb упал)
```
[!] VNC Monitor: Бот 1 - Xvfb не работает (дисплей :101)
[!] VNC Monitor: Бот 1 требует перезапуска (Xvfb упал)
```

В этом случае **бот должен быть перезапущен полностью** (автоматически в `main.py` или вручную для `run_bot.py`).

## Тестирование

### 1. Запустите бота
```bash
python3 run_bot.py 1
```

Ожидаемый вывод:
```
[+] [Бот 1] x11vnc запущен на порту 5901
[+] VNC Monitor: Активирован (monitoring bot 1 on :101)
[+] VNC Monitor: Запущен (мониторинг бота 1 на :101 каждые 10.0 сек)
```

### 2. Проверьте работу VNC
```bash
# Подключитесь через Remmina или vncviewer
vncviewer localhost:5901
```

### 3. Симулируйте падение VNC
```bash
# Убейте процесс x11vnc
pkill -9 -f "x11vnc.*:101"
```

### 4. Наблюдайте за восстановлением
Через **10 секунд** в логе должно появиться:
```
[!] VNC Monitor: Бот 1 - VNC не работает (порт 5901)
[*] VNC Monitor: Перезапуск x11vnc для бота 1 (дисплей :101, порт 5901)
[+] VNC Monitor: x11vnc для бота 1 успешно перезапущен на порту 5901
```

### 5. Проверьте что VNC работает
```bash
# Снова подключитесь
vncviewer localhost:5901
```

## Мониторинг состояния

### Проверка процессов
```bash
# Xvfb и x11vnc процессы
ps aux | grep -E "Xvfb|x11vnc" | grep -v grep

# Пример вывода:
# dmitrii 2921769  0.7  0.1 227396 82428 pts/1 S+  20:51   0:05 Xvfb :101
# dmitrii 2931323  0.0  0.0  36576 17608 pts/1 S+  20:53   0:00 x11vnc -display :101
```

### Проверка портов
```bash
# VNC порты
ss -tlnp | grep 590

# Пример вывода:
# LISTEN 0 32 0.0.0.0:5901 0.0.0.0:* users:(("x11vnc",pid=2931323,fd=8))
```

### Проверка логов
```bash
# Лог VNC Monitor (последние 50 строк)
tail -50 /tmp/bot_1_x11vnc_monitor.log

# Лог x11vnc (последняя попытка запуска)
tail -20 /tmp/bot_1_x11vnc_attempt_0.log
```

## Ручное восстановление (если автоматическое не сработало)

```bash
# Скрипт восстановления
./restore_vnc.sh 1

# Или вручную
DISPLAY=:101 x11vnc -display :101 -rfbport 5901 -nopw -forever -shared \
       -nowf -noxdamage -repeat -q &
```

## Отличие от запуска через `main.py`

### `main.py` (все боты сразу)
```python
# Один VNC Monitor на всех ботов
vnc_monitor = VNCHealthMonitor(bot_count=bots_count, check_interval=30.0)
# Проверяет ботов 0, 1, 2, ..., bots_count-1
```

### `run_bot.py` (один бот)
```python
# Один VNC Monitor на одного бота
vnc_monitor = VNCHealthMonitor(
    bot_count=bot_index + 1,
    check_interval=10.0,
    bot_offset=bot_index  # ← Проверяет только этого бота
)
```

## Конфигурация

### Настройка интервала проверки
```python
# Быстрее реакция, больше нагрузка на CPU
vnc_monitor = VNCHealthMonitor(..., check_interval=5.0)

# Медленнее реакция, меньше нагрузка
vnc_monitor = VNCHealthMonitor(..., check_interval=60.0)
```

### Настройка bot_offset
```python
# Для bot_0
bot_offset=0

# Для bot_3
bot_offset=3

# Для bot_5
bot_offset=5
```

## Возможные проблемы и решения

### Проблема: VNC не восстанавливается
**Симптомы:**
```
[!] VNC Monitor: x11vnc для бота 1 умер сразу после запуска
```

**Причины:**
1. Порт занят другим процессом
2. Xvfb упал
3. Проблемы с памятью

**Решение:**
```bash
# 1. Освободите порт
fuser -k 5901/tcp

# 2. Проверьте Xvfb
ps aux | grep Xvfb.*:101

# 3. Перезапустите бота полностью
pkill -f "run_bot.py 1"
sleep 2
python3 run_bot.py 1
```

### Проблема: Зомби процессы
**Симптомы:**
```
dmitrii 2921829  0.0  0.0      0     0 pts/1 Z+  20:51   0:00 [x11vnc] <defunct>
```

**Решение:**
Зомби процессы не убиваются — это нормально. Они исчезнут когда родительский процесс завершится.

### Проблема: Multiple x11vnc процессов
**Симптомы:**
```
[!] VNC Monitor: Порт 5901 уже занят
```

**Решение:**
```bash
# Убейте все x11vnc для этого дисплея
pkill -9 -f "x11vnc.*:101"

# Подождите 2 секунды
sleep 2

# VNC Monitor автоматически перезапустит
```

## Рекомендации

1. **Используйте `check_interval=10.0`** — баланс между скоростью реакции и нагрузкой
2. **Мониторьте логи** — `tail -f /tmp/bot_*_x11vnc_monitor.log`
3. **Периодически перезапускайте ботов** — для очистки памяти и сброса состояния
4. **Настройте логирование** — сохраняйте логи для анализа падений
