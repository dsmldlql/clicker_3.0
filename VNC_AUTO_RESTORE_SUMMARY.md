# Автоматическое восстановление VNC — Итоговая инструкция

## ✅ Что работает

При запуске бота через `run_bot.py` или `./run_bot_N.sh`:

1. **VNC Monitor запускается автоматически** и проверяет VNC каждые 10 секунд
2. **При падении x11vnc** — автоматически перезапускает его
3. **Восстановление занимает ~2 секунды**

## 🚀 Запуск бота

```bash
# Вариант 1: Shell скрипт (рекомендуется)
./run_bot_3.sh

# Вариант 2: Python напрямую
.venv/bin/python3 run_bot.py 3
```

## 📊 Мониторинг

### Проверка работы бота
```bash
ps aux | grep "run_bot.py 3"
```

### Проверка VNC
```bash
# Порт должен слушаться
ss -tlnp | grep 5903

# Пример вывода:
# LISTEN 0 32 0.0.0.0:5903 0.0.0.0:* users:(("x11vnc",pid=12345,fd=8))
```

### Проверка Xvfb
```bash
ps aux | grep "Xvfb.*:103"
```

### Логи
```bash
# Лог бота (основной)
tail -f /tmp/bot3_test.log

# Лог x11vnc (последняя попытка запуска)
tail -f /tmp/bot_3_x11vnc_attempt_0.log

# Лог VNC Monitor (если нужен)
tail -f /tmp/bot_3_x11vnc_monitor.log
```

## 🔧 Тестирование автоматического восстановления

### Ручной тест
```bash
# 1. Запустите бота
./run_bot_3.sh &

# 2. Подождите 15 секунд
sleep 15

# 3. Проверьте что VNC работает
ss -tlnp | grep 5903

# 4. Убейте x11vnc
pkill -9 -f "x11vnc.*:103"

# 5. Подождите 10-15 секунд
sleep 15

# 6. Проверьте что VNC восстановился
ss -tlnp | grep 5903
# Должен показать новый процесс x11vnc
```

### Автоматический тест
```bash
./test_vnc_auto_restore.sh
```

## 📝 Внесённые изменения

### 1. `scripts/vnc_monitor.py`
- Добавлен параметр `bot_offset` для проверки конкретного бота
- Добавлен трекинг запущенных VNC процессов (`vnc_processes`)
- Исправлена опция запуска x11vnc: `-q` вместо `-loglevel 0`
- Улучшен `monitor_loop()` для работы с `bot_offset`
- Улучшен `stop()` для остановки запущенных VNC процессов

### 2. `run_bot.py`
- Передаёт `bot_offset=bot_index` в `VNCHealthMonitor`
- Увеличен интервал проверки до 10 секунд (было 30)
- Обновлены сообщения для использования `.venv/bin/python3`

### 3. `run_bot_N.sh`
- Обновлены для использования `.venv/bin/python3`

## ⚙️ Конфигурация

### Настройка в `run_bot.py`
```python
vnc_monitor = VNCHealthMonitor(
    bot_count=bot_index + 1,      # Количество ботов для проверки
    check_interval=10.0,          # Интервал проверки (секунды)
    bot_offset=bot_index          # Смещение для конкретного бота
)
```

### Изменение интервала проверки
```python
# Быстрее (чаще проверки, больше CPU)
check_interval=5.0

# Медленнее (реже проверки, меньше CPU)
check_interval=30.0
```

## 🐛 Решение проблем

### VNC не восстанавливается автоматически

**Симптомы:**
```
[!] VNC Monitor: x11vnc для бота 3 умер сразу после запуска
```

**Причины и решения:**

1. **Порт занят**
   ```bash
   # Освободите порт
   fuser -k 5903/tcp
   sleep 2
   
   # VNC Monitor автоматически перезапустит
   ```

2. **Xvfb упал**
   ```bash
   # Проверьте Xvfb
   ps aux | grep "Xvfb.*:103"
   
   # Если не работает — перезапустите бота
   pkill -f "run_bot.py 3"
   sleep 2
   ./run_bot_3.sh
   ```

3. **Недостаточно памяти**
   ```bash
   # Проверьте память
   free -h
   
   # Освободите память или перезапустите бота
   ```

### Зомби процессы x11vnc

**Симптомы:**
```
dmitrii 12345  0.0  0.0  0  0  ?  Z  21:17  0:00 [x11vnc] <defunct>
```

**Решение:**
Это нормально. Зомби процессы исчезнут когда родительский процесс завершится. Не мешают работе.

### Multiple x11vnc процессов

**Симптомы:**
```
[!] VNC Monitor: Порт 5903 уже занят
```

**Решение:**
```bash
# Убейте все x11vnc для этого дисплея
pkill -9 -f "x11vnc.*:103"
sleep 2

# VNC Monitor автоматически перезапустит
```

## 📈 Статистика

### Успешное восстановление
```
[1] Запуск бота 3...
[2] Ожидание запуска (15 секунд)...
[+] Бот запущен, VNC работает на порту 5903
[4] Симуляция падения VNC (убиваем x11vnc)...
[5] Ожидание автоматического восстановления (до 20 секунд)...
[+] VNC восстановлен через 2 секунд!
[6] Результат:
[+] VNC успешно восстановлён!
```

### Время восстановления
- **Обнаружение проблемы:** до 10 секунд (интервал проверки)
- **Перезапуск x11vnc:** ~2 секунды
- **Итого:** 10-12 секунд

## 📚 Дополнительные файлы

- `VNC_AUTO_RESTORE.md` — подробная документация
- `test_vnc_auto_restore.sh` — скрипт автоматического тестирования
- `restore_vnc.sh` — скрипт ручного восстановления VNC

## 🎯 Рекомендации

1. **Используйте `check_interval=10.0`** — баланс между скоростью и нагрузкой
2. **Мониторьте логи** при первом запуске нового бота
3. **Периодически перезапускайте ботов** (раз в 24 часа) для очистки памяти
4. **Настройте алерты** на падение VNC (можно парсить логи)
